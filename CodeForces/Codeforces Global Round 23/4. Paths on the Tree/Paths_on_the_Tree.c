#include <stdio.h>
#include <stdlib.h>

int main()
{
    freopen("Paths_on_the_Tree.txt", "r", stdin);
    int t;
    scanf("%d",t);
    for(int i=0;i<t;i++)
    {
        //TODO: check how to make a dynamically allocated list with malloc, calloc
    }
    return 0;
}