#include <stdio.h>
#include <stdlib.h>

int main()
{
    freopen("Joking_easy.txt", "r", stdin);
    int t;
    scanf("%d", t);
    for (int i = 0; i < t; i++)
    {
        // TODO: check how to make a dynamically allocated list with malloc, calloc
    }
    return 0;
}